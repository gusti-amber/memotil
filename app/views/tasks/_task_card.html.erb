<%= link_to task_path(task) do %>
  <div class="card bg-base-100 border border-subtle
              h-full hover:bg-surface hover:shadow-sm">
    <div class="card-body h-full">
      <div class="flex items-center gap-2 pr-10">
        <!-- ステータスアイコン -->
        <%= render 'tasks/status_icon', task: task %>

        <!-- タイトル -->
        <h2 class="card-title text-base font-normal line-clamp-1"><%= task.title %></h2>
      </div>

      <% if task.description.present? %>
        <p class="text-sm text-muted line-clamp-2"><%= task.description %></p>
      <% end %>

      <!-- タグ一覧 -->
      <div class="card-actions flex-1">
        <%= render partial: "tasks/tag_badge",
                  collection: task.tags,
                  as: :tag,
                  locals: { size: "badge-sm" } %>
      </div>
      <div class="flex justify-start">
        <!-- 更新日時の表示 -->
        <p class="text-muted text-xs">
          <%= updated_ago_label(task.updated_at) %>
        </p>
      </div>
    </div>
  </div>
<% end %>
