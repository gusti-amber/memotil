<% if current_user.guest_user? %>
  <!-- ゲストユーザーの場合の注意書き -->
  <div class="alert alert-info alert-soft mb-1">
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
      <path stroke-linecap="round" stroke-linejoin="round" d="m11.25 11.25.041-.02a.75.75 0 0 1 1.063.852l-.708 2.836a.75.75 0 0 0 1.063.853l.041-.021M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Zm-9-3.75h.008v.008H12V8.25Z" />
    </svg>
    <div class="text-sm text-base-content font-semibold">
      <p>ゲストユーザーはGitHub連携を利用できません。</p>
      <p>この機能を利用するには、一度ログアウトし、メールアドレスまたは GitHub アカウントでログインしてください。</p>
    </div>
  </div>
<% end %>

<!-- GitHub連携カード -->
<%= render 'tasks/github_integration_card', task: task %>

<!-- ボタン群 -->
<div class="flex justify-center">
  <%= render 'tasks/status_button', task: task %>
</div>
<div class="flex justify-center">
  <%= render 'tasks/destroy_button', task: task %>
</div>
