<!-- やることリストカード -->
<div class="card bg-base-200 shadow-sm w-full">
  <div class="card-body">
    <p class="text-neutral font-semibold">やることリスト</p>
    <div class="flex flex-col gap-2">
      <%= render partial: "todos/todo", collection: task.todos %>
    </div>
  </div>
</div>
<!-- ボタン群 -->
<div class="flex flex-col items-start gap-1">
  <%= render 'tasks/status_button', task: task %>
  <%= render 'tasks/destroy_button', task: task %>
</div>
